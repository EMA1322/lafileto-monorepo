C4Context
title Contexto actual de La Fileto
Person(customer, "Cliente", "Consulta el menú y arma un pedido.")
Person(staff, "Personal autorizado", "Administra catálogo, usuarios y configuración.")
System(client, "Client", "React/Vite público con hash routing.")
System(admin, "Admin", "React/Vite interno con autenticación y RBAC.")
System(backend, "Backend API", "Express bajo /api/v1.")
SystemDb(db, "MySQL", "Persistencia operada por Prisma.")
Rel(customer, client, "Usa", "HTTPS")
Rel(staff, admin, "Usa", "HTTPS")
Rel(client, backend, "Consume API pública", "HTTPS/JSON")
Rel(admin, backend, "Consume API protegida", "HTTPS/JSON + Bearer JWT")
Rel(backend, db, "Lee y escribe", "Prisma")
