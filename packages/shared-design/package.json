{
  "name": "@lafileto/design",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "tokens.css"
  ],
  "scripts": {
    "lint": "pnpm exec eslint . --no-error-on-unmatched-pattern",
    "lint:fix": "pnpm exec eslint . --fix --no-error-on-unmatched-pattern",
    "format": "pnpm exec prettier . --write"
  }
}
