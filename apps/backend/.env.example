# ── Runtime ───────────────────────────────────────────────────────────────
# Ambiente de ejecución del servidor Express (development | production).
NODE_ENV=development
# Puerto HTTP donde se expondrá la API (3000 = default del monorepo).
PORT=3000

# ── DB (Prisma) ───────────────────────────────────────────────────────────
# Cadena de conexión completa a MySQL (usuario local de desarrollo).
DATABASE_URL="mysql://lafileto_app:Fileto_2025!@127.0.0.1:3306/lafileto"

# ── Auth (I1-ENDPOINTS) ───────────────────────────────────────────────────
# Se usa para firmar/verificar los JWT de los administradores.
JWT_SECRET=change-me
# Duración del JWT emitido; respetar formato de jsonwebtoken (ej: 15m, 1d).
JWT_EXPIRES_IN=15m
# Credenciales del usuario admin seed (ver prisma/seed.js).
ADMIN_EMAIL=admin@lafileto.ar
ADMIN_FULLNAME="La Fileto Admin"
ADMIN_PASSWORD=ChangeMe!2025

# ── CORS (allowlist dev) ──────────────────────────────────────────────────
# Lista blanca de orígenes permitidos. Separar por comas, sin espacios.
# El backend incluye localhost/127.0.0.1 por defecto; agrega tu IP LAN si lo necesitás.
CORS_ALLOWLIST=http://localhost:5173,http://localhost:5174

# ── Rate limit login ──────────────────────────────────────────────────────
# Ventana del rate-limit en ms para /auth/login (900000 = 15 minutos).
RATE_LIMIT_LOGIN_WINDOW_MS=900000
# Máximo de intentos fallidos dentro de la ventana anterior.
RATE_LIMIT_LOGIN_MAX=10

# ── Límites de request ────────────────────────────────────────────────────
# Límite global de tamaño del JSON para evitar payloads excesivos en dev.
BODY_LIMIT=100kb
# Timeout de request para cortar colgados de red en desarrollo (ms).
REQUEST_TIMEOUT_MS=15000
