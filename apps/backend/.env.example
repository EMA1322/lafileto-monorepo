# ── Runtime ───────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=4000

# ── DB (Prisma) ───────────────────────────────────────────────────────────
# Se configurará en I1-DB-SCHEMA+MIGRATIONS
DATABASE_URL="mysql://user:password@localhost:3306/lafileto"

# ── Auth (I1-ENDPOINTS) ───────────────────────────────────────────────────
# Obligatorio para firmar/verificar JWT.
JWT_SECRET=change-me
# Opcional: default 1d si se omite.
JWT_EXPIRES_IN=15m

# ── CORS (allowlist dev) ──────────────────────────────────────────────────
# Vite Admin/Client por defecto; agrega otros orígenes si los usas
CORS_ALLOWLIST=http://localhost:5173,http://192.168.1.33:5173,http://localhost:5175,http://192.168.1.33:5175,http://192.168.1.33:5174

# ── Rate limit login ──────────────────────────────────────────────────────
RATE_LIMIT_LOGIN_WINDOW_MS=900000   # 15 min
RATE_LIMIT_LOGIN_MAX=10             # 10 req / ventana por IP

# ── Límites de request ────────────────────────────────────────────────────
BODY_LIMIT=100kb
REQUEST_TIMEOUT_MS=15000
