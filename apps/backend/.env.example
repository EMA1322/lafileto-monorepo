# ── Runtime ───────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=4000

# ── DB (Prisma) ───────────────────────────────────────────────────────────
# Se configurará en I1-DB-SCHEMA+MIGRATIONS
DATABASE_URL="mysql://lafileto_app:Fileto_2025!@127.0.0.1:3306/lafileto"

# ── Auth (I1-ENDPOINTS) ───────────────────────────────────────────────────
# Obligatorio para firmar/verificar JWT.
JWT_SECRET=change-me
# Opcional: default 1d si se omite.
JWT_EXPIRES_IN=15m

# ── CORS (allowlist dev) ──────────────────────────────────────────────────
# Separa por comas y sin espacios extra.
# Ejemplo: admin/client locales + IP LAN para pruebas.
CORS_ALLOWLIST=http://localhost:5174,http://localhost:5173,http://192.168.1.33:5174

# ── Rate limit login ──────────────────────────────────────────────────────
RATE_LIMIT_LOGIN_WINDOW_MS=900000   # 15 min
RATE_LIMIT_LOGIN_MAX=10             # 10 req / ventana por IP

# ── Límites de request ────────────────────────────────────────────────────
BODY_LIMIT=100kb
REQUEST_TIMEOUT_MS=15000
