// Estado compartido para entidades activables.
// Comentarios en español, código en inglés.

export const STATUS = Object.freeze({
  ACTIVE: 'ACTIVE',
  INACTIVE: 'INACTIVE',
});

export const STATUS_LIST = Object.freeze([STATUS.ACTIVE, STATUS.INACTIVE]);
