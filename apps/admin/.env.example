# ── Variables de Vite (se cargan en dev y build) ─────────────────────────────
# Prefijo base para la API del backend. En dev usamos el proxy `/api` → 3000.
VITE_API_BASE=/api
# Fuente de datos para los módulos que soportan mocks: 'json' (default) o 'api'.
VITE_DATA_SOURCE=api
